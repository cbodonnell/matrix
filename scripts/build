#!/bin/bash

sudo rsync -av --progress ./ /etc/networks --exclude scripts --exclude working --exclude Jenkinsfile --delete
cd /etc/networks/networks
sudo python3 venv venv
source venv/bin/activate
sudo pip install -r requirements.txt
